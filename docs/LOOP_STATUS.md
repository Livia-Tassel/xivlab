# xivLab Loop Status

**Last updated**: 2026-05-04T19:25:00+08:00
**Last completed Task**: T02 (Database Foundation + All Schemas — alembic, sqlite-vec, all ORM models)
**Next Task**: T03 (Auth — Password Hashing + Register Endpoint)
**Test suite**: green (6 passed)
**Last commit**: pending — see git log after this iteration

## Notes for next iteration

- All 16 ORM tables are created via `alembic/versions/0001_initial_schema.py`. The sqlite-vec virtual table `paper_vectors` (with helper tables `paper_vectors_chunks/info/rowids/vector_chunks00`) is created via raw `op.execute(...)` in the same migration since autogen doesn't detect virtual tables.
- 9 prompt categories are seeded by `alembic/versions/0002_seed_categories.py`. Current alembic head is `0002_seed_categories`.
- `app/db.py` defines `attach_sqlite_extensions(sync_engine)` so both the app's runtime engine **and** alembic's per-run engine load `sqlite-vec`. The connect-event handler drills through aiosqlite's async wrapper to reach the underlying `sqlite3.Connection` for `enable_load_extension` (which the async wrapper exposes as a coroutine that can't be awaited from inside a sync event handler).
- Tests in `tests/unit/test_models.py` and `tests/unit/test_migrations.py` run against the live `data/app.db`. Test isolation is via try/finally cleanup of inserted rows. **For the next iteration** that hits `tests/`, consider adding a fixture that wraps each test in a transaction-rollback if state pollution becomes painful.
- pytest still emits `DeprecationWarning: datetime.datetime.utcnow() is deprecated` (1 warning). The plan uses `datetime.utcnow` as Python-side defaults; a future cleanup task can switch to `datetime.now(UTC)` everywhere — the migration's stored values aren't TZ-aware so behavior is unchanged. Not blocking.
- The seed migration uses parameterized `bind.exec_driver_sql(...)` instead of the plan's f-string concatenation. Functionally identical for this seed but safer (and avoids quoting issues with Chinese text). **Plan deviation: deliberate.**
- The autogenerated migration was renamed from `<hash>_initial_schema.py` to `0001_initial_schema.py` and the `revision` constant was rewritten to `"0001_initial_schema"` so the seed migration's `down_revision = "0001_initial_schema"` resolves.

## Bash classifier note

Did not recur in this iteration; ran cleanly.

## Open issues / TODOs surfaced this iteration

- (none — Task 02 is fully self-contained)

## What's next (T03 high-level reminder)

T03 implements password hashing + the registration endpoint:
- `app/services/password.py` — bcrypt wrappers (`hash_password`, `verify_password`) using `passlib[bcrypt]`. Cost 12 (passlib default).
- `app/services/tokens.py` — `secrets.token_urlsafe(32)` helpers
- `app/schemas/auth.py` — Pydantic models for register request/response
- `app/routers/auth.py` — POST `/api/v1/auth/register` (creates User, sends verification email)
- Tests: `tests/integration/test_auth_register.py` (happy path, duplicate email, weak password)
- The email verification token row is created here (table is already in place from T02).
- Email send is async-mocked (`EMAIL_BACKEND=mock` is the test default).
